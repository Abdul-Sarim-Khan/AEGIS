# Copy this file to .streamlit/secrets.toml for LOCAL development only.
# On Streamlit Community Cloud, set this in the dashboard's "Secrets" manager
# instead -- never commit the real key.
# Used in Phase 3.

GEMINI_API_KEY = "your-google-gemini-api-key-here"
